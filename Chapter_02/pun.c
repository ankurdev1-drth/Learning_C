#include <stdio.h>
int main(void) // starting of the program
{
	printf("Brevity is the soul of wit.\n --Shakespeare\n");
	return 0;
	// declarations:
	int height;
	int length;
	int width;
	// statements:
	height = 8;
	length = 10;
	width = 6;
	int volume;
	volume = height * width * length;  // volume =  480 after multiplication
	printf("Volume: %d\n", volume);
}

/* This is a comment 
    Wrote it just to test the lengths of the comments tag lol 
 ima doing anything at this point okey back to work ! 
*/
// jarvis is this also a comment huh? , hell yeah it is hehe
// we can create variables like :  int height, length, width, volume;
// float profit, loss; 
// we can assign em values like: height = 8; 
/* so the final order to follow for the assigning of the variables is:
	int height;
	height=8;  */

// the reverse is fales 
// for float values :
/*  float profit;
	profit = 235.53f; */
